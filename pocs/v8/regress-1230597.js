function main() {
  
  
  for(var i=0; i<2; i++) {
    if(i==1) { }
    with ({}) {
      try {
        var07().foo = 1;
      } catch { }
    }
  }

  
  try {
    
    
    
    
    
    
    var10() = var08(1, ...foo, 2);
  } catch { }
}


%PrepareFunctionForOptimization(main);
main();
%OptimizeFunctionOnNextCall(main);
main();
