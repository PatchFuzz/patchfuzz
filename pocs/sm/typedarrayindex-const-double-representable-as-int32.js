function f(n) {
  const ta = new Int32Array(n);

  
  
  
  const r = n === 0 ? undefined : 0;

  
  const k = numberToDouble(0);

  for (var i = 0; i < 10; ++i) {
    print(ta[k], r);
  }
}

for (var i = 0; i < 2; ++i) {
  f(i);
}
