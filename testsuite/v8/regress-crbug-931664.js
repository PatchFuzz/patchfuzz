





function opt(){
  for(l in('a')){
    try{
      for(a in('')) {
        for(let arg2 in(+(arg2)));
      }
    }
    finally{}
  }
}
%PrepareFunctionForOptimization(opt);
opt();
%OptimizeFunctionOnNextCall(opt);
opt();
