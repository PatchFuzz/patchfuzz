













for (b = 0; b < 100; b++) {
    var setv = new Set();
    setv.add(1);
    setv.add(Math.SQRT2);
    setv.forEach(function(value, key, set) {
        setv.clear();
        (eval)("" + 123);
    });
}
