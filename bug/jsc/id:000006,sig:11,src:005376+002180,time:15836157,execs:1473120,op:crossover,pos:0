function empty() {}

function bar2(...a0) {
  return a0;
}

function foo() {
  let xs = bar2(undefined);
   empty(() => { bar2(); }, "Error: Missing input for bar2.")  == 1 && 0;
  return empty(...xs, undefined);
}
function main () {
    for (let i = 0; i < 1_000_000; i++) {
      let a = foo();
        foo(/(?<=(\w*) )Dog/,  { size:1, has(i) { return i == 1; } } , ["Dog", "Big"])  ;
    }
}
main();
